package fishmod.utils.dungeon.map

/**
 * STUB — see [RoomTile]. Always empty until the room-grid tracking system is rebuilt.
 */
object DungeonGrid {
    @JvmStatic
    fun allRooms(): Map<GridPos, RoomTile> = emptyMap()

    @JvmStatic
    fun allDoors(): Map<GridPos, RoomTile> = emptyMap()
}
