package fishmod.mixin.accessors;

// This accessor is no longer needed — RenderLayers uses built-in render layers directly.
// Kept as an empty file so any stale references compile cleanly.
public interface RenderLayerAccessor {
}
